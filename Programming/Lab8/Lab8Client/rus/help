Информация о командах
Команда help: вывести справку
по доступным командам

Команда info: вывести в стандартный
поток вывода информацию о коллекции

Команда show: вывести в стандартный
поток вывода все элементы коллекции
в строковом представлении

Команда add: добавить новый элемент в коллекцию

Команда update id: обновить значение элемента коллекции,
id которого равен заданному

Команда remove_by_id: удалить элемент
из коллекции по его id

Команда clear: очистить коллекцию
Команда execute_script: : считать и исполнить скрипт
из указанного файла. В скрипте содержатся команды в таком же виде,
в котором их вводит пользователь в интерактивном режиме.

Команда add_if_max: добавить
новый элемент в коллекцию, если его значение
превышает значение наибольшего элемента этой коллекции

Команда remove_lower: удалить
из коллекции все элементы, меньшие, чем заданный
по полю height

Команда history: вывести
последние 10 команд (без их аргументов)

Команда Search: позволяет найти объекты в коллекции
поля которых равны заданным или начинаются с них

Команда filter_by_height: вывести элементы, значение
поля height которых равно заданному

Команда print_unique_location: вывести уникальные
значения поля location

Для редактирования своих элементво в базе необходимо указывать
все необходимые поля вверху таблицы(id, name и т.д.)

Формат задания полей:
id - целое число, name - строка, coordinates -
число типа int и число типа double,
height - число типа double, passportId - строка до 20 символов,
hairColor - [GREEN, WHITE, BROWN], nationality -
[JAPAN, VATICAN,THAILAND],
location - число типа float и два числа типа long, user -
ваш логин(не обязательно);

Формат поиска по таблице:
В тех же самых ячейках, что и заданий полей, записываете то,
что хотите найти - таблица всё сделает за вас.

Фильтры сбрасываются после каждой команды, которая взаимодействует с Базой данных